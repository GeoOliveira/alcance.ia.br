import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
const migration=readFileSync("supabase/migrations/202607200024_whatsapp_link_manager.sql","utf8");
describe("WhatsApp manager migration",()=>{it("mantém recurso e Google desligados",()=>{expect(migration).toMatch(/resource_whatsapp_link_manager[\s\S]{0,180}false/);expect(migration).toMatch(/whatsapp_manager_google_login[\s\S]{0,180}false/);expect(migration).toMatch(/whatsapp_manager_google_one_tap[\s\S]{0,180}false/)});it("aplica RLS e isolamento pelo auth uid",()=>{for(const table of["user_profiles","account_consents","user_whatsapp_links","user_link_metric_cache","user_activity_logs"])expect(migration).toContain(`alter table public.${table} enable row level security`);expect(migration).toMatch(/whatsapp_links_own_select[\s\S]*user_id = \(select auth\.uid\(\)\)/)});it("não concede acesso anônimo às tabelas do usuário",()=>{expect(migration).toMatch(/revoke all on public\.user_profiles[\s\S]*from anon, authenticated/)})});
