import Link from "next/link"; import { requireAdminSession } from "@/lib/admin/auth"; import { listPocRuns } from "@/lib/social-providers/scrape-creators/execution"; import { AdminPageHeader, AdminStatusBadge } from "@/components/admin/admin-ui";
export default async function RunsPage() { await requireAdminSession("provider_poc.view"); const runs = await listPocRuns(100); return <><AdminPageHeader eyebrow="HISTÓRICO" title="Execuções da POC" description="Resultados técnicos sanitizados, limitados às 100 execuções mais recentes." /><section className="admin-panel"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Data</th><th>Endpoint</th><th>Entrada</th><th>Status</th><th>HTTP</th><th>Tempo</th><th>Itens</th><th>Retries</th></tr></thead><tbody>{runs.map((run) => <tr key={run.id}><td><Link href={`/admin/integracoes/scrapecreators/execucoes/${run.id}`}>{new Date(run.created_at).toLocaleString("pt-BR")}</Link></td><td>{run.endpoint}</td><td className="admin-mono">{run.input_identifier}</td><td><AdminStatusBadge status={run.status} /></td><td>{run.http_status ?? "—"}</td><td>{run.duration_ms} ms</td><td>{run.items_count}</td><td>{run.retries_count}</td></tr>)}</tbody></table></div></section></>; }
