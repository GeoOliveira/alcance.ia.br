import { notFound } from "next/navigation";import { getUserLink } from "@/lib/whatsapp-manager/data";import { EditLinkForm } from "@/components/whatsapp-manager/edit-link-form";
export default async function EditLinkPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const link=await getUserLink(id);if(!link)notFound();return <><header className="panel-heading"><div><span>EDITAR LINK</span><h1>Organize “{link.title}”</h1><p>Esta versão altera apenas metadados locais seguros.</p></div></header><EditLinkForm id={link.id} title={link.title} labels={link.labels}/></>}
