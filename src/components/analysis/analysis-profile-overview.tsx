import { formatCompactNumber, formatDecimal } from "@/lib/analysis/analysis-formatters";
import type { AnalysisViewModel } from "@/lib/analysis/types";
export function AnalysisProfileOverview({ analysis }: { analysis: AnalysisViewModel }) { const p = analysis.profile!, m = analysis.metrics!; return <section className="analysis-section"><div className="analysis-section-heading"><span>01</span><div><h2>Visão geral do perfil</h2><p>Informações públicas disponíveis no momento da coleta.</p></div></div><div className="analysis-overview-grid"><div className="analysis-bio-card"><span>Bio pública</span><p>{p.biography || "Bio não disponível."}</p>{p.externalUrl ? <a href={p.externalUrl} target="_blank" rel="noreferrer">Visitar link da bio ↗</a> : <small>Nenhum link público identificado.</small>}</div><dl className="analysis-facts"><div><dt>Seguidores</dt><dd>{formatCompactNumber(p.followersCount)}</dd></div><div><dt>Seguindo</dt><dd>{formatCompactNumber(p.followingCount)}</dd></div><div><dt>Publicações</dt><dd>{formatCompactNumber(p.postsCount)}</dd></div><div><dt>Relação seguidores/seguindo</dt><dd>{formatDecimal(m.followersFollowingRatio, 1)}</dd></div><div><dt>Categoria</dt><dd>{p.category || "Não disponível"}</dd></div><div><dt>Conta</dt><dd>{p.isVerified ? "Verificada · " : ""}{p.isPrivate ? "Privada" : "Pública"}</dd></div></dl></div></section>; }
