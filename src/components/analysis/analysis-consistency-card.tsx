import { formatDecimal } from "@/lib/analysis/analysis-formatters";
import type { AnalysisViewModel } from "@/lib/analysis/types";
export function AnalysisConsistencyCard({ analysis }: { analysis: AnalysisViewModel }) { const m = analysis.metrics!, max = Math.max(m.postsLast30Days, 1); return <section className="analysis-section analysis-split"><div><div className="analysis-section-heading"><span>03</span><div><h2>Consistência de publicação</h2><p>Distribuição recente baseada nas datas disponíveis.</p></div></div><div className="analysis-consistency-status"><strong>{m.consistencyLabel}</strong><span>{m.averageIntervalDays === null ? "Intervalo indisponível" : `Intervalo médio de ${formatDecimal(m.averageIntervalDays)} dias`}</span></div></div><div className="analysis-bars"><div><span>Últimos 7 dias <b>{m.postsLast7Days}</b></span><i><em style={{ width: `${Math.min(100, m.postsLast7Days / max * 100)}%` }} /></i></div><div><span>Últimos 30 dias <b>{m.postsLast30Days}</b></span><i><em style={{ width: "100%" }} /></i></div><p>{m.postsLast30Days >= 12 ? "O perfil apresentou atividade frequente no período." : m.postsLast30Days >= 4 ? "O perfil apresentou atividade recorrente no período." : "A atividade recente observada foi baixa."}</p></div></section>; }
