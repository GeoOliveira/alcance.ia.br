import Image from "next/image";
import Link from "next/link";
import { AnalyticsLink } from "@/components/analytics/analytics-link";
import { formatDateTime } from "@/lib/analysis/analysis-formatters";
import type { AnalysisViewModel } from "@/lib/analysis/types";
export function AnalysisHeader({ analysis }: { analysis: AnalysisViewModel }) { const profile = analysis.profile; return <header className="analysis-report-header"><div className="analysis-report-actions"><Link href="/">← Voltar</Link><AnalyticsLink href="/#analisar" eventName="analysis_refresh_clicked" properties={{ request_id: analysis.requestId, cta_location: "analysis_header" }}>Nova análise</AnalyticsLink></div><div className="analysis-identity">{profile?.profileImageUrl ? <Image src={profile.profileImageUrl} alt={`Foto pública de @${analysis.username}`} width={76} height={76} /> : <span className="analysis-avatar-fallback" aria-hidden="true">@</span>}<div><span className="analysis-status-pill">{analysis.state === "partial" ? "Resultado parcial" : analysis.state === "insufficient_data" ? "Amostra reduzida" : "Análise concluída"}</span><h1>{profile?.displayName || `@${analysis.username}`}</h1><a href={analysis.profileUrl} target="_blank" rel="noreferrer">@{analysis.username} ↗</a></div></div><dl><div><dt>Analisado em</dt><dd>{formatDateTime(analysis.analyzedAt || analysis.requestedAt)}</dd></div><div><dt>Publicações avaliadas</dt><dd>{analysis.posts.length}</dd></div><div><dt>Fonte</dt><dd>Dados públicos {analysis.isCached ? "em cache" : "recentes"}</dd></div></dl></header>; }
