import type { AnalysisMetrics } from "@/lib/analysis/types";
export function AnalysisContentFormatBreakdown({ metrics }: { metrics: AnalysisMetrics }) { const formats = [{ label: "Reels", value: metrics.reelsCount }, { label: "Imagens", value: metrics.imagesCount }, { label: "Carrosséis", value: metrics.carouselsCount }]; const total = Math.max(metrics.analyzedPosts, 1); return <section className="analysis-section"><div className="analysis-section-heading"><span>04</span><div><h2>Formatos de conteúdo</h2><p>{metrics.contentLabel} · formato mais usado: {metrics.mostUsedFormat || "não disponível"}.</p></div></div><div className="analysis-format-grid">{formats.map((format) => <article key={format.label}><div><span>{format.label}</span><strong>{format.value}</strong></div><i><em style={{ width: `${format.value / total * 100}%` }} /></i><small>{Math.round(format.value / total * 100)}% da amostra</small></article>)}</div>{metrics.bestObservedFormat && <p className="analysis-method-note">Melhor desempenho médio observado: <strong>{metrics.bestObservedFormat}</strong>, comparando curtidas e comentários disponíveis.</p>}</section>; }
