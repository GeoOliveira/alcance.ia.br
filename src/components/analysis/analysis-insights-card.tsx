import type { AnalysisObservation } from "@/lib/analysis/types";
export function AnalysisInsightsCard({ observations }: { observations: AnalysisObservation[] }) { return <section className="analysis-section"><div className="analysis-section-heading"><span>06</span><div><h2>Observações iniciais</h2><p>Leituras simples produzidas por regras transparentes — sem IA nesta etapa.</p></div></div><div className="analysis-observations">{observations.map((item) => <article data-tone={item.tone} key={item.id}><span aria-hidden="true">{item.tone === "positive" ? "✓" : item.tone === "attention" ? "!" : "→"}</span><div><h3>{item.title}</h3><p>{item.message}</p></div></article>)}</div></section>; }
