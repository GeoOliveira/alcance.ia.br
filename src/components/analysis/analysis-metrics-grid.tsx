import { formatCompactNumber, formatDecimal, formatPercentage } from "@/lib/analysis/analysis-formatters";
import type { AnalysisMetrics } from "@/lib/analysis/types";
export function AnalysisMetricsGrid({ metrics }: { metrics: AnalysisMetrics }) { const items = [
  ["Média de curtidas", formatCompactNumber(metrics.averageLikes)], ["Mediana de curtidas", formatCompactNumber(metrics.medianLikes)], ["Média de comentários", formatDecimal(metrics.averageComments)], ["Mediana de comentários", formatDecimal(metrics.medianComments)],
  ["Engajamento estimado", formatPercentage(metrics.estimatedEngagementRate)], ["Média de views em Reels", formatCompactNumber(metrics.averageReelViews)], ["Conteúdos analisados", String(metrics.analyzedPosts)], ["Reels analisados", String(metrics.reelsCount)],
]; return <section className="analysis-section"><div className="analysis-section-heading"><span>02</span><div><h2>Métricas principais</h2><p>Médias calculadas somente com valores públicos conhecidos; ausência não é convertida em zero.</p></div></div><div className="analysis-metric-grid">{items.map(([label, value]) => <article key={label}><span>{label}</span><strong>{value}</strong></article>)}</div><p className="analysis-method-note">O engajamento estimado usa a média de curtidas + comentários dividida pelos seguidores disponíveis. É uma estimativa limitada à amostra pública.</p></section>; }
