import type { BrandedContentPlatform, BrandedContentResult } from "@/lib/meta/branded-content/types";
export function ResultCard({ result, platform, onOpen }: { result: BrandedContentResult; platform: BrandedContentPlatform; onOpen?: () => void }) {
  const date = result.creationDate ? new Intl.DateTimeFormat("pt-BR", { dateStyle: "long", timeZone: "UTC" }).format(new Date(result.creationDate)) : "Data não informada";
  return <article className="branded-result-card"><header><span>{platform === "instagram" ? "Instagram" : "Facebook"}</span><strong>{result.typeLabel}</strong><time dateTime={result.creationDate || undefined}>{date}</time></header><div><small>Criador</small><h3>{result.creator?.name || "Criador não informado"}</h3>{result.creator?.username && <p>@{result.creator.username}</p>}</div><div><small>Parceiros</small>{result.partners.length ? <ul>{result.partners.map((partner, index) => <li key={partner.id || partner.profileUrl || `${partner.name}-${index}`}>{partner.name || partner.username || "Parceiro não informado"}{partner.username ? ` · @${partner.username}` : ""}</li>)}</ul> : <p>Nenhum parceiro informado na resposta.</p>}</div>{result.contentUrl && <a className="button button-small" href={result.contentUrl} target="_blank" rel="noopener noreferrer" onClick={onOpen}>Ver publicação <span aria-hidden="true">↗</span><span className="sr-only"> (abre em nova aba)</span></a>}</article>;
}
