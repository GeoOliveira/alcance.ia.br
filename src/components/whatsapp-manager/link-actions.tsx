"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { ManagerIcon } from "./icons";
export function CopyLinkButton({url}:{url:string}){const[copied,setCopied]=useState(false);return <button className="panel-secondary" onClick={async()=>{await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1800)}}><ManagerIcon name="copy"/>{copied?"Copiado":"Copiar"}</button>}
export function ArchiveLinkButton({id,archived=false}:{id:string;archived?:boolean}){const router=useRouter();const[pending,setPending]=useState(false);return <button className="panel-secondary" disabled={pending} onClick={async()=>{setPending(true);try{const response=await fetch(`/api/painel/links/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({archived:!archived})});if(response.ok){router.push("/painel/links");router.refresh()}}finally{setPending(false)}}}><ManagerIcon name="archive"/>{pending?"Aguarde…":archived?"Desarquivar":"Arquivar"}</button>}
