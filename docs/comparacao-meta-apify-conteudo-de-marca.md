# Comparação Meta × Apify

Nenhuma comparação empírica foi executada em 15/07/2026 porque não havia token Apify local nem sessão autenticada no Console. Não há base para declarar um provedor melhor.

O modo `admin_compare` calcula quantidade, chaves em comum, URLs/itens exclusivos, sobreposição, duração e custo estimado. Ele só funciona no admin quando modo, settings, flags, ambiente e recurso permitem. A lista não mescla os datasets.

Após o primeiro teste real, registrar aqui consulta sanitizada, plataforma, período, limite, duração, custo, campos ausentes e diferenças de cobertura — sem username, run ID, dataset ID ou resposta bruta.
